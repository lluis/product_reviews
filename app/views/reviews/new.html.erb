<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New review</h1>

  <%= render "form", product: @product, review: @review %>

  <%= link_to "Back to reviews", product_reviews_path(@product), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
